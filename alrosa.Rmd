---
title: "Alrosa"
output:
  html_document:
    self_contained: yes
---

<style type="text/css">
.main-container {
  max-width: 1920px;
  margin-left: auto;
  margin-right: auto;
}
</style>

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
knitr::opts_chunk$set(message = FALSE)
knitr::opts_chunk$set(warning = FALSE)
```

# Libraries
```{r}
library("ggplot2")
library("reshape2")
library("ggfortify")
library("tidyverse")

source("functions.R")
```


```{r}
data <-read.csv("data/alrosa/revenue.csv", sep = ",")
  
# avoid sorting
data$Dt <- factor(data$Dt, levels = data$Dt)
data
  
df <- data

revenuePlot(df)

```

# Basic observation of the data Alrosa/Revenue

```{r, fig.width = 15}
#revenuePlot("data/alrosa/revenue.csv")


  


```
